default: true
# Line length — MkDocs admonitions and tables go long
MD013: false
# Allow duplicate headings (common in MkDocs nav sections)
MD024: false
# Allow raw HTML (MkDocs Material uses it for grids, badges, etc.)
MD033: false
# Allow inline HTML in first line (MkDocs sometimes needs it)
MD041: false
# Ordered list style — allow any
MD029:
  style: ordered
